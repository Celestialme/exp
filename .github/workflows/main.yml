name: "Test"
on:
  workflow_dispatch:
jobs:
  compile-rust:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: install Rust stable
      uses: actions-rs/toolchain@v1
      with:
        toolchain: stable
    - run: cargo build --release
    - uses: actions/upload-artifact@v3
      with:
        name: my-artifact
        path: target/release/nixos



